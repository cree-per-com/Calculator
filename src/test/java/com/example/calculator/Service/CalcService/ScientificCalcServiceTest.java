/*
package com.example.calculator.Service.CalcService;

import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ScientificCalcServiceTest {

    @Test
    void cirCalcProc() {
        ScientificCalcService scientificCalcService = new ScientificCalcService(new FourBasicCalcService());
        String whichcir = "tan";
        double value = Math.PI/4;
        String result = scientificCalcService.CirCalcProc(whichcir, String.valueOf(value));
        Assertions.assertThat(result).isEqualTo("1");
    }
}



 */